#!/bin/bash

current_date_time=$(date)
rc_file=$1
tag_name=$2
echo "$current_date_time Starting deployment of $tag_name using $rc_file for credentials."

# creating network



# creating subnet

subnet_list=$(openstack subnet list --tag $tag_name -f value)
echo "$subnet_list"